import React from 'react';
// This component has been removed in the current version.
// Keeping a placeholder to avoid breaking builds if imported elsewhere by mistake.
export const CameraFeed = () => null;
export type CameraFeedHandle = any;
